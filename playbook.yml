- hosts: localhost
  tasks:
    - name: compare multiple environments
      EagleEye: 
         folder: dir
         exclude:
           - local
           - remote
         file: env.yml
      register: res
      become: true
       

    - debug: var=res  
