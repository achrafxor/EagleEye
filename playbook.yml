- hosts: localhost
  tasks:
    - name: test2
      EagleEye: 
         folder: dir
         exclude:
           - local
           - remote
         file: env.yml
      register: res
      become: true
       

    - debug: var=res  
